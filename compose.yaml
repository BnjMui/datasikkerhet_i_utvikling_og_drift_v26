name: db_datasikkerhet

services:
  db:
    image: mysql
    container_name: mysql_db
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: datasikkerhet
      MYSQL_ROOT_PASSWORD: dev
    volumes:
      - datasikkerhet_db:/var/lib/mysql

volumes:
  datasikkerhet_db:
